<main class="flex-1 p-6 bg-gray-50">
    <div class="flex items-center justify-between mb-6">
        <h1 class="text-2xl font-semibold text-gray-800">Modifier l'entrée de Stock pour : <?= htmlspecialchars($product->NOM_PRODUIT ?? 'Produit inconnu') ?></h1>
    </div>

    <?= $this->flash->output() ?>

    <div class="bg-white p-6 rounded-lg shadow-md">
        <form action="<?= $this->url->get('stocks/edit/' . $stock->ID_STOCK) ?>" method="POST">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Produit</label>
                    <p class="mt-1 text-lg text-gray-900"><?= htmlspecialchars($product->NOM_PRODUIT ?? 'Produit inconnu') ?></p>
                    <p class="text-sm text-gray-500">ID Produit: <?= htmlspecialchars($stock->ID_PRODUIT) ?></p>
                </div>

                <div>
                    <label for="quantite_stock" class="block text-sm font-medium text-gray-700 mb-1">Quantité en Stock</label>
                    <input type="number" name="quantite_stock" id="quantite_stock" value="<?= htmlspecialchars($stock->QUANTITE_STOCK) ?>" min="0" class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-primary focus:border-primary sm:text-sm" required>
                </div>

                <div>
                    <label for="date_misejour" class="block text-sm font-medium text-gray-700 mb-1">Date de dernière mise à jour</label>
                    <input type="datetime-local" name="date_misejour" id="date_misejour" value="<?= (new DateTime($stock->DATE_MISEJOUR))->format('Y-m-d\TH:i') ?>" class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-primary focus:border-primary sm:text-sm">
                    <p class="mt-1 text-sm text-gray-500">Laissez vide pour utiliser la date et l'heure actuelles.</p>
                </div>
            </div>

            <div class="mt-6 flex justify-end space-x-3">
                <a href="<?= $this->url->get('stocks') ?>" class="inline-flex justify-center py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary">
                    Annuler
                </a>
                <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-primary hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary">
                    Mettre à jour le Stock
                </button>
            </div>
        </form>
    </div>
</main>