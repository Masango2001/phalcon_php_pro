<main class="flex-1 p-6 bg-gray-50">
    <div class="flex items-center justify-between mb-6">
        <h1 class="text-2xl font-semibold text-gray-800">Modifier le client : <?= htmlspecialchars($client->NOM_CLIENT . ' ' . $client->PRENOM_CLIENT) ?></h1>
    </div>

    <?= $this->flash->output() ?>

    <div class="bg-white p-6 rounded-lg shadow-md">
        <form method="post" action="<?= $this->url->get('clients/edit/' . $client->ID_CLIENT) ?>">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
                <div>
                    <label for="client-first-name" class="block text-sm font-medium text-gray-700 mb-1">Prénom</label>
                    <input type="text" id="client-first-name" name="prenom_client" value="<?= htmlspecialchars($client->PRENOM_CLIENT) ?>" required class="w-full px-3 py-2 border border-gray-300 rounded-button focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent">
                </div>
                <div>
                    <label for="client-last-name" class="block text-sm font-medium text-gray-700 mb-1">Nom</label>
                    <input type="text" id="client-last-name" name="nom_client" value="<?= htmlspecialchars($client->NOM_CLIENT) ?>" required class="w-full px-3 py-2 border border-gray-300 rounded-button focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent">
                </div>
            </div>
            <div class="mb-6">
                <label for="client-phone" class="block text-sm font-medium text-gray-700 mb-1">Téléphone</label>
                <input type="tel" id="client-phone" name="telephone_client" value="<?= htmlspecialchars($client->TELEPHONE_CLIENT ?? '') ?>" required class="w-full px-3 py-2 border border-gray-300 rounded-button focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent">
            </div>
            <div class="mb-6">
                <label for="client-address" class="block text-sm font-medium text-gray-700 mb-1">Adresse</label>
                <input type="text" id="client-address" name="adresse_client" value="<?= htmlspecialchars($client->ADRESSE_CLIENT ?? '') ?>" class="w-full px-3 py-2 border border-gray-300 rounded-button focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent">
            </div>
            <div class="flex justify-end space-x-3">
                <a href="<?= $this->url->get('clients') ?>" class="inline-flex justify-center py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary">
                    Annuler
                </a>
                <button type="submit" class="inline-flex justify-center py-2 px-4 bg-primary text-white rounded-md shadow-sm text-sm font-medium hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary">
                    Enregistrer
                </button>
            </div>
        </form>
    </div>
</main>